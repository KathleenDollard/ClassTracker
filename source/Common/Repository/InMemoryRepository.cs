using System;
using System.Collections.Generic;
using System.Collections.Immutable;

namespace Common
{
    // TODO: As an exercise, create either an in-memory or JSON repository base
    //       These are quite useful early in development

    //public class InMemoryRepository <T>
    //{
    //    private ImmutableDictionary<Type, T> _entries { get; }

    //    protected InMemoryRepository(Dictionary<Type, T> entries)
    //    {
    //        _entries = entries.ToImmutableDictionary();
    //    }

    //    public T this [Type type] =>_entries.GetValueOrDefault(type);
    //}
}
